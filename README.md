# PDF Merge Tool

Flask + PyPDF を使った PDF 結合アプリです。

## 🚀 セットアップ
コマンドに以下を入力すれば作動される

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt 



